<!--view.ejs-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/stylesheets/view.css">
  <title>Files</title>
</head>
<body>
  <h1>Your Uploaded files</h1>
  <% if (file.length > 0) { %>
    <ul>
      <% file.forEach(file => { %>
        <li>
          <a href="/view/<%= file.id %>"><%= file.filename %></a>
          <form action="/view/<%= file.id %>?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit" onclick="return confirm('Are you sure you want to delete this file?');">Delete</button>
          </form>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No files available. <a href="/upload">Add one</a></p>
  <% } %>  
  <a href="/">Home</a> 
</body>
</html>
